[project]
name = "to-do-list"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.3",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "faker>=40.19.1",
]
